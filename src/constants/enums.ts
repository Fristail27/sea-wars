export enum CellValueEnum {
    empty = 0,
    miss = 1,
    ship = 2,
    kill = 3,
    support = 4
}


export enum FieldTypesEnum {
    self = 'self',
    enemy = 'enemy'
}

export enum ShipDirectionEnum {
    vert = 'vertical',
    hor = 'horizontal',
}
